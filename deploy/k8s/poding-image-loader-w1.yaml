apiVersion: v1
kind: Pod
metadata:
  name: poding-image-loader-w1
  namespace: poding-system
spec:
  restartPolicy: Never
  hostPID: true
  nodeName: k8s-w1
  containers:
    - name: loader
      image: busybox:1.36
      imagePullPolicy: IfNotPresent
      command: ["sh", "-c", "sleep 3600"]
      securityContext:
        privileged: true
      volumeMounts:
        - name: host-root
          mountPath: /host
  volumes:
    - name: host-root
      hostPath:
        path: /
        type: Directory
